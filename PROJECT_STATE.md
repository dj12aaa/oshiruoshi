# OSHIRU — 再開用の状態

更新: 2026-09-10 UTC。対象は OSHIRU のみ。別アプリを作成・変更しない。

## 現在の目的とフェーズ

利用者が正しい商品を探し、条件を理解して販売元へ進める状態を改善する。現在は V24 後のガイド修繕前チェックポイント。市場や収益の再設計より、ユーザーが報告した誤字・読みにくさ・注釈の重なりを優先する。

## 確認した状態

- GitHub: https://github.com/dj12aaa/oshiruoshi
- 本番: https://oshiruoshi.vercel.app
- ガイド: https://oshiruoshi.vercel.app/how-to-use
- 2026-09-10 に GitHub 接続で読み取った main: `bddb2d3f4c4300f6ac7eaff31883252ee0c9c8db`
- V24 製品コミット: `5eae6db9c7e7ccc00cb927117b9834f1d42addba`
- 検証根拠: `verification/release-v24.json`。観測日は 2026-09-09。現在の稼働・在庫・順位の再測定と混同しない。
- 再開時のローカル基準: `5836415`、ブランチ `work/oshiru-v19-usability`。ローカルと main の SHA は同一ではない。
- 使用技術: 静的 HTML/CSS/JavaScript、Node.js ESM API、Vercel、GitHub Actions、Node test、Playwright。
- デプロイ状態: 最後の確認済み製品は V24。今回のガイド修繕は未実装・未公開。
- DB 状態: 今回 DB 接続・変更は実施していない。保存検索等は端末内保存。DB が存在しないとまでは断定しない。

## 完了済み — 作り直さない

- 初回と本検索の完了前に最終 0 件を表示しない。取得済みの部分結果、12 秒の全体期限、Yahoo・Google への通常検索を保持。
- どの検索語でも同じ語で 8 サイトへ進む外部検索導線。API 接続と過去出品を区別。
- 最新ページの商品 API 画像取得、表示中 5 分更新、自動スライド・停止・カテゴリ切替。
- robots、sitemap、初期 HTML の SEO 本文・構造化データ。
- 変更対象外を削除しないリリース tree 検査。

## 作業中・次の 5 件

1. `how-to-use.html/css/js` の低解像度スクリーンショットと絶対位置の注釈を、生きた HTML 文字と操作図へ置換。正例は「五条悟 アクスタ」。
2. API 未接続・過去情報・画像解析の条件・最新取得と発売日の違いなど、既存の正しい説明と導線を保持。
3. 誤字、注釈の重なり、320〜1440px、200% 文字拡大、フォーム・目次・FAQ の検査を追加。元の検索用誤字回帰テストは削除しない。
4. Astra 独立レビューと検査を通した単位を PR 化。現行 main を基準に tree を照合してから統合。
5. 正規本番 URL のガイド版・文字・操作・スマホ表示を確認して初めて修繕を完了と記録。

## 未解決・既知のバグ

- ガイドの「アクスラ」は、誤字補正を試す検査画像を説明画像へ転用したため。小さい画面全体を縮小し、番号・矢印を重ねたことで可読性も低下。V24 の機能テストは文字品質を検査していなかった。
- メルカリ・Yahoo!フリマ等の一般出品 API は新規接続していない。過去出品と外部検索であり、ライブ API と称さない。
- 楽天の取得制限・認可問題、Search Console の認可と実計測、アフィリエイト登録・成果・入金は未確認。
- 収益、DAU/WAU/MAU、CVR、継続率、CAC/LTV、純利益は未計測。ゼロとは記載しない。

## モデルと利用枠

- ユーザー指定: Astra 極高または Pro。低性能モデルへのフォールバックは禁止。
- 今回の独立レビューは `gpt-6-astra` / `xhigh` を明示して依頼したが、実査前に中断。独立レビューは未実施。親実行や将来の予約実行のモデルが同じことを証明する情報ではない。
- 取得できるツールには個人の Work 利用枠残量・解除時刻の読取 API がない。予約作成にも model / reasoning 指定項目がない。
- 自動化状態: **未登録**。解除監視・指定モデルでの自動再開は、必要な確認手段とモデル固定が未確立。時間経過だけで解除済みと判定しない。
- `ASTRA_REVIEW_REQUIRED`: 修正後ガイドの独立最終レビュー。将来のモデル固定・再開確認手段。

## 環境変数 — 名称だけ

ソース内で参照を確認した名称。実値・設定済み状態・全名称の網羅を保証しない。

`YAHOO_CLIENT_ID`, `RAKUTEN_APP_ID`, `RAKUTEN_ACCESS_KEY`, `RAKUTEN_AFFILIATE_ID`, `RAKUTEN_ICHIBA_ENDPOINT`, `OPENAI_API_KEY`, `OPENAI_MODEL`, `X_BEARER_TOKEN`, `PUBLIC_CONTACT_EMAIL`, `PUBLIC_SITE_URL`, `PUBLIC_SITE_INDEXABLE`, `ENABLE_PUBLIC_PAGE_ADAPTERS`, `VERCEL_ENV`, `OSHIRU_FEED_LIVE`, `OSHIRU_TEST_BASE_URL`。

## 直近の判断

利用制限の回避・無制限実行は行わない。モデル条件を確認できない予約を「Astra 自動再開」として登録しない。代わりに、作業を小さく分け、状態を Git に保存し、差分だけから再開する。ユーザー価値・安全性・確認済み根拠を優先し、未確認の収益や検索順位を実績にしない。

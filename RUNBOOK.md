# OSHIRU RUNBOOK

## 再開手順

1. PROJECT_STATE.md → ROADMAP.md / TODO.md / DECISIONS.md / METRICS.md → AGENTS.md / docs/QUALITY_LESSONS.md を読む。保存済みの完了作業は再生成しない。
2. 実行モデルがユーザー条件の Astra 極高または Pro であることを確認する。別モデルへ自動降格しない。親、子、将来の予約実行の設定を混同しない。
3. GitHub の現在 main、進行中 PR・CI、ローカル `git status --short` を読み取る。別の実行が同じタスクを進めていれば新しい書込を始めない。
4. 最後の検証記録と公開 URL を照合。稼働の簡単な確認と、変更後の回帰検査を区別する。
5. TODO の最上位の未完了項目を一つ選び、開始前の基準 SHA・対象ファイル・完了条件・次の操作を checkpoint に保存する。
6. 変更 → 対象検査 → 必要な既存回帰 → 独立レビュー → 保存を小さい単位で行う。失敗したら原因を直し、同じ検査を無根拠に繰り返さない。

## 利用制限・再開の境界

- 残量、解除時刻、解除済み状態を推測しない。待った時間や一般の料金表だけでは証明にならない。
- 制限の前兆・長い作業・重要変更の前には安全な状態を保存。突然停止に備え、チェックポイントは終了直前だけに作らない。
- 制限時は別アカウント・秘密情報の転用・無断の API 課金・低性能モデルで回避しない。
- 現在の予約ツールは model / reasoning を指定できず、Work 個人利用枠 API もない。このため解除監視・指定モデル自動再開は **未設定**。
- 将来設定する場合も、正式な確認手段とモデル固定が成立してから。時間ベース確認は最大毎時、同時実行を避け、条件未成立なら製品変更・デプロイをしない。解除時刻の瞬間に再開できるとは約束しない。
- 既存の別プロジェクトの自動化や、停止中の古い OSHIRU Vercel ビルド制限タスクを勝手に流用・再開しない。Work 利用枠と Vercel ビルド枠は別。

## 保存と公開

- ローカル編集は apply_patch。秘密値、Cookie、トークン、個人情報は Git に入れない。
- V24 のローカルブランチをそのまま main と同じと扱わない。公開変更は最新 main を基準にする。
- GitHub tree を組み立てる場合、実在する 40 桁の base tree SHA と完全な再帰 tree を必須とする。`scripts/validate-release-tree.mjs` で実際の候補 tree とローカル manifest を照合し、合格前に ref を更新しない。
- コミット・PR・Vercel Preview・本番を別々に記録。すでに成功した同一リリースを無意味に再デプロイしない。
- 公開前: `node --test tests/*.test.mjs`（npm test の実体）、変更 JS の `node --check`、`git diff --check`、必要なブラウザ検査。独立した lint/typecheck/build script がない構成では、存在しない検査を成功扱いしない。
- 公開後: 正規ドメインの対象版、API、対象 UI を確認。ガイド変更は小さい画面・文字拡大・実際の検索遷移まで確認。CI 成功だけで完了しない。
- 元に戻す必要があれば対象コミットを明示した可逆的 revert をレビュー。ユーザーの別変更を巻き戻す reset や force push をしない。

## 今回の再開地点

ガイド製品コードはまだ V24。修正案は PROJECT_STATE.md の次の 5 件。まずユーザーが指摘したガイドの修繕を実装し、既存検索の誤字入力テスト自体は残す。テスト用の不正入力画面を説明画像に再利用しない。
